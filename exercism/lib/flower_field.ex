# defmodule FlowerField do
#   @doc """
#   Annotate empty spots next to flowers with the number of flowers next to them.
#   """
#   @spec annotate([String.t()]) :: [String.t()]

#   def annotate(board) do
#     Enum.map(board, fn chunk -> nil end)
#     |> Enum.with_index()
#   end
# end
